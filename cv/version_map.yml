firmware:
  # Don't touch! Generating in CI.
  version: main
  ## Components
  qemu: 9.2.0
  libvirt: 10.9.0
  edk2: stable202411

module:
  # Don't touch! Generating in CI.
  version: main
  # Should be updated manual.
  firmwareMinSupportedVersion: "v0.17.0"
