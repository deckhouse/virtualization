---
apiVersion: v1
kind: Service
metadata:
  name: jump-host
  namespace: {{ .Values.namespace }}
  labels:
    infra: jump-host
spec:
  type: NodePort
  selector:
    app: jump-host
  ports:
    - name: ssh
      protocol: TCP
      port: 2222
      targetPort: 2222

