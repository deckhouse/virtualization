apiVersion: virtualization.deckhouse.io/v1alpha3
kind: VirtualMachineClass
metadata:
  name: {{ include "infra.vmclass-name" . }}
  labels:
    {{- include "infra.test-label" . | nindent 4 }}
spec:
  cpu:
    type: Discovery
