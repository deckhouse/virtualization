apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - vm-bar.yaml
  - vd-bar-root.yaml
configurations:
  - transformer.yaml
generatorOptions:
  disableNameSuffixHash: true
secretGenerator:
  - files:
      - userData=cfg/cloudinit-bar.yaml
    name: cloud-init-bar
    type: provisioning.virtualization.deckhouse.io/cloud-init
