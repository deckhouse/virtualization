apiVersion: virtualization.deckhouse.io/v1alpha2
kind: VirtualDisk
metadata:
  name: vd-from-cvi-alpine-http
spec:
  persistentVolumeClaim:
    storageClassName: "{{ .STORAGE_CLASS_NAME }}"
    size: 360Mi
  dataSource:
    type: ObjectRef
    objectRef:
      kind: ClusterVirtualImage
      name: v12n-e2e-alpine-bios
