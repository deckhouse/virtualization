phantom:
  address: 95.143.190.246:80 # [Target's address]:[target's port]
  uris:
    - /
  load_profile:
    load_type: rps # schedule load by defining requests per second
    schedule: line(1, 50, 2m) # starting from 1rps growing linearly to 10rps during 10 minutes
#  writelog: all
console:
  enabled: true # enable console output
telegraf:
  enabled: false # let's disable telegraf monitoring for the first time
