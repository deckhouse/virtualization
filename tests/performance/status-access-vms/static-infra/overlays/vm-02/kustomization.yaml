apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

nameSuffix: "-vm-02"

labels:
  - includeSelectors: true
    pairs:
      vm: vm-02

resources:
  - ../../base/vm

patches:
  - target:
      kind: VirtualMachineIPAddressClaim
      name: static
    patch: |-
      - op: replace
        path: /spec/address
        value: 10.66.10.122
