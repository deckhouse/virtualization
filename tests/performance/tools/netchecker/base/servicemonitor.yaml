apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: netchecker-monitor
  namespace: netchecker
  labels:
      prometheus: main
spec:
  selector:
    matchLabels:
      app: netchecker
  endpoints:
  - port: metrics
    path: /metrics
    interval: 30s
