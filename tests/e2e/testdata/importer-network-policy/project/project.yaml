apiVersion: deckhouse.io/v1alpha2
kind: Project
metadata:
  name: project
spec:
  parameters:
    administrators: []
    resourceQuota:
      requests:
        cpu: 20
        memory: 20Gi
    networkPolicy: Isolated
  projectTemplateName: default
