
{{- $version := get $.PackageVersion $.ImageName }}
{{- $gitRepoUrl := "linux-rdma/rdma-core.git" }}

{{- $name := print $.ImageName "-dependencies" -}}
{{- define "$name" -}}
altPackages:
- gcc git make libtool libnl-devel ninja-build
- binutils cmake pkgconfig
- pandoc
- python3-module-Cython python3-devel python3-module-docutils
- libpcap-devel
- tree
packages:
- systemd libnl3 rdma-core
{{- end -}}

{{ $builderDependencies := include "$name" . | fromYaml }}


---
image: {{ .PackagePath }}/{{ $.ImageName }}
final: false
fromImage: builder/scratch
import:
- image: {{ .PackagePath }}/{{ $.ImageName }}-builder
  add: /out
  to: /{{ .ImageName }}
  before: setup
  excludePaths:
  - etc/init.d

---
image: {{ .PackagePath }}/{{ .ImageName }}-src-artifact
final: false
fromImage: builder/src
secrets:
- id: SOURCE_REPO
  value: {{ .PACKAGE_CLONE_REPO }}
shell:
  install:
  - git clone --depth=1 $(cat /run/secrets/SOURCE_REPO)/{{ $gitRepoUrl }} --branch {{ $version }} /src

---
image: {{ .PackagePath }}/{{ $.ImageName }}-builder
final: false
fromImage: builder/alt
import:
- image: {{ .PackagePath }}/{{ .ImageName }}-src-artifact
  add: /src
  to: /src
  before: install
shell:
  beforeInstall:
  {{- include "alt packages proxy" . | nindent 2 }}
  - |
    apt-get install -y \
      {{ $builderDependencies.altPackages | join " " }}

  {{- include "alt packages clean" . | nindent 2 }}

  install:
  - |
    OUTDIR=/out

    cd /src

    # Fix typo in rdma-core v53.0 pyverbs source that breaks Cython parsing:
    # f'{<Mlx5FlowActionAttr>attr).attr.type}.' -> f'{(<Mlx5FlowActionAttr>attr).attr.type}.'
    sed -i "s|f'{<Mlx5FlowActionAttr>attr).attr.type}\.'|f'{(<Mlx5FlowActionAttr>attr).attr.type}.'|" \
      /src/pyverbs/providers/mlx5/mlx5dv_flow.pyx

    cmake -GNinja \
      -DCMAKE_BUILD_TYPE=Release \
      -DCMAKE_INSTALL_PREFIX=/usr \
      -DCMAKE_INSTALL_SYSCONFDIR=/etc \
      -DCMAKE_INSTALL_LIBDIR=/usr/lib64 \
      -DNO_MAN_PAGES=ON

    ninja -j$(nproc)
    DESTDIR=$OUTDIR ninja install
    cp -r kernel-headers/rdma $OUTDIR/usr/include/
    tree -hp $OUTDIR
