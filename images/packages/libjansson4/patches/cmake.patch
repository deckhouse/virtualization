--- jansson/CMakeLists.txt      2025-08-22 18:09:31.111529976 +0300
+++ jansson-alt/CMakeLists.txt  2025-08-22 18:13:21.797132655 +0300
@@ -20,9 +20,6 @@
 endif ()

 # Set some nicer output dirs.
-set(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR}/bin)
-set(CMAKE_LIBRARY_OUTPUT_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR}/lib)
-set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR}/lib)
 set(JANSSON_TEMP_DIR ${CMAKE_CURRENT_BINARY_DIR}/tmp)

 # Give the debug version a different postfix for windows,
@@ -583,7 +580,7 @@
 if(WIN32 AND NOT CYGWIN)
   set(DEF_INSTALL_CMAKE_DIR cmake)
 else()
-  set(DEF_INSTALL_CMAKE_DIR lib/cmake/jansson)
+  set(DEF_INSTALL_CMAKE_DIR ${JANSSON_INSTALL_LIB_DIR}/cmake/jansson)
 endif()

 set(JANSSON_INSTALL_CMAKE_DIR ${DEF_INSTALL_CMAKE_DIR} CACHE PATH "Installation directory for CMake files")
@@ -593,7 +590,7 @@
 #  have to defined the same variables used there).
 set(prefix      ${CMAKE_INSTALL_PREFIX})
 set(exec_prefix "\${prefix}")
-set(libdir      "\${exec_prefix}/${JANSSON_INSTALL_LIB_DIR}")
+set(libdir      ${JANSSON_INSTALL_LIB_DIR})
 set(includedir  "\${prefix}/${JANSSON_INSTALL_INCLUDE_DIR}")
 set(VERSION     ${JANSSON_DISPLAY_VERSION})
 configure_file(${CMAKE_CURRENT_SOURCE_DIR}/jansson.pc.in
@@ -634,10 +631,10 @@
 if (JANSSON_INSTALL)
   install(TARGETS jansson
           EXPORT janssonTargets
-          LIBRARY DESTINATION "lib"
-          ARCHIVE DESTINATION "lib"
-          RUNTIME DESTINATION "bin"
-          INCLUDES DESTINATION "include")
+          LIBRARY DESTINATION ${JANSSON_INSTALL_LIB_DIR}
+          ARCHIVE DESTINATION ${JANSSON_INSTALL_LIB_DIR}
+          RUNTIME DESTINATION ${JANSSON_INSTALL_BIN_DIR}
+          INCLUDES DESTINATION ${JANSSON_INSTALL_INCLUDE_DIR})

   install(FILES ${JANSSON_HDR_PUBLIC}
           DESTINATION "include")
@@ -645,7 +642,7 @@
   # Install the pkg-config.
   install(FILES
           ${CMAKE_CURRENT_BINARY_DIR}/jansson.pc
-          DESTINATION lib/pkgconfig)
+          DESTINATION ${JANSSON_INSTALL_LIB_DIR}/pkgconfig)

   # Install the configs.
   install(FILES
