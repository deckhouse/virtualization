# libxcrypt
```
└── usr
    ├── include
    │   ├── crypt.h
    │   └── xcrypt.h
    ├── lib64
    │   ├── libcrypt.a
    │   ├── libcrypt.la
    │   ├── libcrypt.so -> libcrypt.so.1.1.0
    │   ├── libcrypt.so.1 -> libcrypt.so.1.1.0
    │   ├── libcrypt.so.1.1.0
    │   ├── libxcrypt.a -> libcrypt.a
    │   ├── libxcrypt.so -> libcrypt.so
    │   └── pkgconfig
    │       ├── libcrypt.pc -> libxcrypt.pc
    │       └── libxcrypt.pc
    └── share

6 directories, 11 files
```