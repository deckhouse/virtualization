# libisofs
```
└── usr
    ├── include
    │   └── libisofs
    │       └── libisofs.h
    └── lib64
        ├── libisofs.a
        ├── libisofs.la
        ├── libisofs.so -> libisofs.so.6.92.0
        ├── libisofs.so.6 -> libisofs.so.6.92.0
        ├── libisofs.so.6.92.0
        └── pkgconfig
            └── libisofs-1.pc
```