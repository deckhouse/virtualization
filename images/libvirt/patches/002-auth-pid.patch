diff --git a/src/remote/remote_daemon.c b/src/remote/remote_daemon.c
index 522aad2177..62c2560ba4 100644
--- a/src/remote/remote_daemon.c
+++ b/src/remote/remote_daemon.c
@@ -62,6 +62,8 @@
 
 #include "virgdbus.h"
 
+#include "auth_pid.h"
+
 VIR_LOG_INIT("daemon." DAEMON_NAME);
 
 #ifndef SOCK_PREFIX
@@ -724,6 +726,7 @@ daemonUsage(const char *argv0, bool privileged)
         { "-p | --pid-file <file>", N_("Change name of PID file") },
         { "-A | --no-admin-srv", N_("Disable admin server startup")},
         { "-R | --no-ro-srv", N_("Disable read-only server startup")},
+        { "-P | --auth-pid <pid>", N_("Restrict socket access to the specified PID") },
     };
 
     fprintf(stderr, "\n");
@@ -811,6 +814,7 @@ int main(int argc, char **argv) {
     
     bool no_admin_srv = false;
     bool no_ro_srv = false;
+    int auth_pid = -1;
 
     struct option opts[] = {
         { "verbose", no_argument, &verbose, 'v' },
@@ -825,6 +829,7 @@ int main(int argc, char **argv) {
         { "help", no_argument, NULL, 'h' },
         {"no-admin-srv", no_argument, NULL, 'A'},
         {"no-ro-srv", no_argument, NULL, 'R'},
+        {"auth-pid", required_argument, NULL, 'P'},
         { 0, 0, 0, 0 },
     };
 
@@ -903,6 +908,13 @@ int main(int argc, char **argv) {
         case 'R':
             no_ro_srv = true;
             break;
+
+        case 'P':
+            if (virStrToLong_i(optarg, NULL, 10, &auth_pid) < 0) {
+                fprintf(stderr, _("Invalid PID value: %s\n"), optarg);
+                exit(EXIT_FAILURE);
+                }
+            break;
             
         case '?':
         default:
@@ -911,6 +923,8 @@ int main(int argc, char **argv) {
         }
     }
 
+    SetAuthPid(auth_pid);
+
     if (optind != argc) {
         fprintf(stderr, "%s: unexpected, non-option, command line arguments\n",
                 argv[0]);
diff --git a/src/rpc/auth_pid.c b/src/rpc/auth_pid.c
new file mode 100644
index 0000000000..1279ad07fb
--- /dev/null
+++ b/src/rpc/auth_pid.c
@@ -0,0 +1,11 @@
+#include "auth_pid.h"
+
+static int auth_pid = -1; 
+
+void SetAuthPid(int p) {
+    auth_pid = p;
+}
+
+int GetAuthPid(void) {
+    return auth_pid;
+}
diff --git a/src/rpc/auth_pid.h b/src/rpc/auth_pid.h
new file mode 100644
index 0000000000..06251aa3d0
--- /dev/null
+++ b/src/rpc/auth_pid.h
@@ -0,0 +1,9 @@
+#pragma once
+
+#ifndef AUTH_PID_H
+#define AUTH_PID_H
+
+void SetAuthPid(int p);
+int GetAuthPid(void);
+
+#endif
diff --git a/src/rpc/meson.build b/src/rpc/meson.build
index 68aaf24b2a..3dc1d211c5 100644
--- a/src/rpc/meson.build
+++ b/src/rpc/meson.build
@@ -48,7 +48,7 @@ endif
 rpc_gen_headers = []
 rpc_gen_sources = []
 
-foreach name : [ 'virnet', 'virkeepalive' ]
+foreach name : [ 'virnet', 'virkeepalive', 'auth_pid' ]
   protocol_file = files('@0@protocol.x'.format(name))
   header_file = '@0@protocol.h'.format(name)
   source_file = '@0@protocol.c'.format(name)
diff --git a/src/rpc/virnetsocket.c b/src/rpc/virnetsocket.c
index e8fc2d5f7d..3c8ba49141 100644
--- a/src/rpc/virnetsocket.c
+++ b/src/rpc/virnetsocket.c
@@ -48,6 +48,7 @@
 #include "virprobe.h"
 #include "virprocess.h"
 #include "virstring.h"
+#include "auth_pid.h"
 
 #if WITH_SSH2
 # include "virnetsshsession.h"
@@ -2060,6 +2061,7 @@ int virNetSocketAccept(virNetSocket *sock, virNetSocket **clientsock)
     virSocketAddr localAddr = { 0 };
     virSocketAddr remoteAddr = { 0 };
     int ret = -1;
+    int expect_auth_pid = -1;
 
     virObjectLock(sock);
 
@@ -2086,6 +2088,21 @@ int virNetSocketAccept(virNetSocket *sock, virNetSocket **clientsock)
         goto cleanup;
     }
 
+    expect_auth_pid = GetAuthPid();
+
+    if (expect_auth_pid >= 0) {
+        struct ucred cred;
+        socklen_t cred_len = sizeof(struct ucred);
+        if (getsockopt(fd, SOL_SOCKET, SO_PEERCRED, &cred, &cred_len) < 0) {
+            virReportSystemError(errno, "%s", _("Unable to get peer credentials for unix socket connection"));
+            goto cleanup;
+        }
+        if (cred.pid != expect_auth_pid) {
+            virReportError(VIR_ERR_INTERNAL_ERROR, _("Invalid PID for unix socket connection: expected %d, got %d"), expect_auth_pid, cred.pid);
+            goto cleanup;
+        }
+    }
+
     if (!(*clientsock = virNetSocketNew(&localAddr,
                                         &remoteAddr,
                                         true,
