---
# Source https://github.com/kubevirt/containerized-data-importer/blob/main/hack/build/config.sh#L31
{{- $builderImage := "quay.io/kubevirt/kubevirt-cdi-bazel-builder:2409041205-8c58c938b" }}
{{- $version := "1.60.3" }}
{{- $goVersion := "1.22.7" }}

image: {{ $.ImageName }}-builder
final: false
from: {{ $builderImage }}
shell:
  install:
  - echo "gimme.sh"
  - cat /etc/profile.d/gimme.sh
  - echo "gimme.sh end"
  - export
  - source /etc/profile.d/gimme.sh && go version
  - export
  - echo "Install new Go version"
  - export GIMME_GO_VERSION={{ $goVersion }}
  - export GOPATH="/go"
  - export GO111MODULE="on"
  - export
  - |
    rm -rf /gimme && rm -rf /tmp/gimme && \
    mkdir -p /gimme && curl -sL \
      https://raw.githubusercontent.com/travis-ci/gimme/master/gimme | \
    HOME=/gimme bash >> /etc/profile.d/gimme.sh
  - ls -la /gimme/.gimme/versions
  - du -shc /gimme/.gimme/versions/*
  - find / -name go
  - echo "cat gimme.sh"
  - cat /etc/profile.d/gimme.sh
  - echo "cat gimme.sh end"
  - export
  - source /etc/profile.d/gimme.sh && go version
  - export
docker:
  ENV:
    GIMME_GO_VERSION: "{{ $goVersion }}"
    GOPATH: "/go"
    GO111MODULE: "on"

#  ENV GOPATH="/go" GOBIN="/usr/bin" GO111MODULE="on"

---
image: {{ $.ImageName }}
final: false
fromImage: {{ $.ImageName }}-builder
git:
  - add: /images/{{ $.ImageName }}
    to: /
    stageDependencies:
      setup:
      - '**/*'
    includePaths:
    - patches
    - unpack-bundle.sh
    excludePaths:
    - patches/README.md
shell:
  setup:
  - ls -la /gimme/.gimme/versions
  - du -shc /gimme/.gimme/versions/*
  - find / -name go
  - echo "cat gimme.sh"
  - cat /etc/profile.d/gimme.sh
  - echo "cat gimme.sh end"
  - export
  - source /etc/profile.d/gimme.sh && go version
  - export

  - which go
  - echo path=$PATH
  - source /etc/profile.d/gimme.sh
  - git clone --depth 1 --branch v{{ $version }} https://github.com/kubevirt/containerized-data-importer.git /containerized-data-importer
  - cd /containerized-data-importer
  - |
    for p in /patches/*.patch ; do
      echo -n "Apply ${p} ... "
      git apply  --ignore-space-change --ignore-whitespace ${p} && echo OK || (echo FAIL ; exit 1)
    done
  - echo "========== Build CDI images ============"
  - /entrypoint.sh make bazel-build-images DOCKER=0
  - |
    find / -type f | grep .tar
  - du -shc /*
  - du -shc /containerized-data-importer/*
  - ls -la /containerized-data-importer/bazel-bin
  - du -shc /containerized-data-importer/bazel-bin/*
  - find /containerized-data-importer

  - echo "Calculate garbage size after first build"
  - du -shc /root/.cache /containerized-data-importer /tmp/*

  - echo "====== Create tar bundle with CDI images ========"
  - /entrypoint.sh bazel build --config=x86_64 --define container_prefix=kubevirt --define image_prefix= --define container_tag=latest //:container-images-bundle.tar
  - echo "======  Unpack CDI images into /  ======"
  - tar tf /containerized-data-importer/bazel-bin/container-images-bundle.tar
  - tar -C / --one-top-level -xf /containerized-data-importer/bazel-bin/container-images-bundle.tar
  - du -shc /*
  - du -shc /containerized-data-importer/*
  - mkdir -p /images && cd /images && /unpack-bundle.sh /container-images-bundle/
  - du -shc /images/*
  - du -shc /container-images-bundle/*

  # reduce image size (~12Gb)
  - echo "Calculate garbage size"
  - du -sh /root/.cache /container-images-bundle/* /containerized-data-importer/* /tmp/*
  - rm -rf /root/.cache
  - rm -rf /container-images-bundle
  - rm -rf /containerized-data-importer
  - rm -rf /tmp/*
