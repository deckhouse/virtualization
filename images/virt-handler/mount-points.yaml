dirs:
  - /etc/virt-handler/clientcertificates
  - /etc/virt-handler/servercertificates
  - /kubeconfig.local
  - /profile-data
  - /etc/podinfo
  - /pods
  - /var/lib/kubevirt
  - /var/lib/kubelet/device-plugins
  - /var/lib/kubelet/pods
  - /var/lib/kubevirt-node-labeller
  # Create dirs in /run, as /var/run is s symlink to /run.
  - /run/kubevirt
  - /run/kubevirt-libvirt-runtimes
  - /run/kubevirt-private
  # Other mount points used in Pod, but created by containerd.
  # - /tmp
