---
image: {{ include "ImageName" (tuple . .ImageName) }}
fromImage: {{ include "ImageName" (tuple . "distroless") }}
import:
- image: {{ include "ImageName" (tuple . "cdi-artifact") }}
  add: /cdi-binaries
  to: /usr/bin
  before: setup
  includePaths:
  - cdi-apiserver
imageSpec:
  config:
    entrypoint: ["/usr/bin/cdi-apiserver", "-alsologtostderr"]
    user: 64535
