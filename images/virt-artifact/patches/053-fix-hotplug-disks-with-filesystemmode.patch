diff --git a/pkg/virt-handler/hotplug-disk/mount.go b/pkg/virt-handler/hotplug-disk/mount.go
index 034c3d8526..4a2e7e7c5f 100644
--- a/pkg/virt-handler/hotplug-disk/mount.go
+++ b/pkg/virt-handler/hotplug-disk/mount.go
@@ -101,6 +101,13 @@ var (
 		return virt_chroot.MountChroot(sourcePath, targetPath, false).CombinedOutput()
 	}
 
+	mountChroot = func(sourcePath, targetPath *safepath.Path) error {
+		opts := []string{
+			"bind", "uid=107", "gid=107",
+		}
+		return virt_chroot.MountChrootWithOptions(sourcePath, targetPath, opts...)
+	}
+
 	unmountCommand = func(diskPath *safepath.Path) ([]byte, error) {
 		return virt_chroot.UmountChroot(diskPath).CombinedOutput()
 	}
@@ -540,8 +547,8 @@ func (m *volumeMounter) mountFileSystemHotplugVolume(vmi *v1.VirtualMachineInsta
 				return err
 			}
 		}
-		if out, err := mountCommand(sourcePath, target); err != nil {
-			return fmt.Errorf("failed to bindmount hotplug volume source from %v to %v: %v : %v", sourcePath, target, string(out), err)
+		if err := mountChroot(sourcePath, target); err != nil {
+			return fmt.Errorf("failed to bindmount hotplug volume source from %v to %v: %v", sourcePath, target, err)
 		}
 		log.DefaultLogger().V(1).Infof("successfully mounted %v", volume)
 	}
diff --git a/pkg/virt-launcher/virtwrap/manager.go b/pkg/virt-launcher/virtwrap/manager.go
index 200ec52e90..c3b46822aa 100644
--- a/pkg/virt-launcher/virtwrap/manager.go
+++ b/pkg/virt-launcher/virtwrap/manager.go
@@ -1276,11 +1276,17 @@ func (l *LibvirtDomainManager) syncDiskHotplug(
 	}
 	// Look up all the disks to attach
 	for _, attachDisk := range getAttachedDisks(spec.Devices.Disks, domain.Spec.Devices.Disks) {
-		allowAttach, err := checkIfDiskReadyToUse(getSourceFile(attachDisk))
+		sourceFile := getSourceFile(attachDisk)
+		allowAttach, err := checkIfDiskReadyToUse(sourceFile)
 		if err != nil {
 			return err
 		}
 		if !allowAttach {
+			diskName := ""
+			if attachDisk.Alias != nil {
+				diskName = attachDisk.Alias.GetName()
+			}
+			logger.Reason(fmt.Errorf("disk %s is not ready to use", diskName)).Errorf("Skipping disk %s. SourceFile: %q", diskName, sourceFile)
 			continue
 		}
 		logger.V(1).Infof("Attaching disk %s, target %s", attachDisk.Alias.GetName(), attachDisk.Target.Device)
@@ -1445,6 +1451,7 @@ func checkIfDiskReadyToUseFunc(filename string) (bool, error) {
 	// Before attempting to attach, ensure we can open the file
 	file, err := os.OpenFile(filename, os.O_RDWR, 0600)
 	if err != nil {
+		log.DefaultLogger().V(1).Infof("Unable to open file: %v", err)
 		return false, nil
 	}
 	if err := file.Close(); err != nil {
