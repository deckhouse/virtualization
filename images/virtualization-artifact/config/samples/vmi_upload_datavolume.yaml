apiVersion: virtualization.deckhouse.io/v1alpha2
kind: VirtualImage
metadata:
  name: vmi-upload-dv
spec:
  storage: Kubernetes
  persistentVolumeClaim:
    storageClass: "local-path"
  dataSource:
    type: "Upload"
