# Just stub file
firmware:
  # Don't touch! Generating in CI.
  version: main
  ## Components
  qemu: 0.0.0
  libvirt: 0.0.0
  edk2: stable000

module:
  # Don't touch! Generating in CI.
  version: main
  # Should be updated manual.
  firmwareMinSupportedVersion: "v0.17.0"
