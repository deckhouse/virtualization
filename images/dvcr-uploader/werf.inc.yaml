---
image: {{ include "ImageName" (tuple . .ImageName) }}
fromImage: {{ include "ImageName" (tuple . "distroless") }}
import:
- image: {{ include "ImageName" (tuple . "dvcr-artifact" "bins") }}
  add: /relocate
  to: /
  after: install
  excludePaths:
  - '**/dvcr-importer'
  - '**/dvcr-cleaner'
imageSpec:
  config:
    workingDir: "/"
    cmd: ["/usr/local/bin/dvcr-uploader"]
    user: 64535
