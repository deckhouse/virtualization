---
image: {{ .ImageName }}
fromImage: distroless
git:
  {{- include "image mount points" . }}
import:
- image: cdi-artifact
  add: /cdi-binaries
  to: /usr/bin
  includePaths:
  - cdi-operator
  before: setup
imageSpec:
  config:
    entrypoint: ["/usr/bin/cdi-operator"]
    user: 64535
