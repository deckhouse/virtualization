diff --git a/ui/vnc-enc-tight.c b/ui/vnc-enc-tight.c
index 41f559e..8fe09d3 100644
--- a/ui/vnc-enc-tight.c
+++ b/ui/vnc-enc-tight.c
@@ -1404,11 +1404,14 @@ static int send_sub_rect_nojpeg(VncState *vs, int x, int y, int w, int h,
     int ret;
 
     if (colors == 0) {
-        if (tight_detect_smooth_image(vs, w, h)) {
-            ret = send_gradient_rect(vs, x, y, w, h);
-        } else {
-            ret = send_full_color_rect(vs, x, y, w, h);
-        }
+        /* No gradient filter: noVNC, which the web console is built on, does not
+         * implement it and drops the connection with "Gradient filter not
+         * implemented" as soon as such a rect arrives. A photo-like wallpaper on a
+         * Windows desktop is enough to trigger it, and the whole console dies.
+         * Plain full-colour data is understood by every client. Frequently updated
+         * smooth areas are unaffected: those take the JPEG path in
+         * send_sub_rect_jpeg and never reach this function. */
+        ret = send_full_color_rect(vs, x, y, w, h);
     } else if (colors == 1) {
         ret = send_solid_rect(vs);
     } else if (colors == 2) {
