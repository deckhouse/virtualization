apiVersion: v1
kind: Service
metadata:
  name: kubevirt-operator-webhook-proxy
  namespace: d8-{{ .Chart.Name }}
  {{- include "helm_lib_module_labels" (list . (dict "app" "virt-operator")) | nindent 2 }}
spec:
  ports:
    - port: 443
      protocol: TCP
      targetPort: 24192
  selector:
    kubevirt.io: virt-operator
  sessionAffinity: None
  type: ClusterIP
