apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  creationTimestamp: null
  labels:
    heritage: deckhouse
    module: virtualization
    rbac.deckhouse.io/aggregate-to-role: user
    rbac.deckhouse.io/kind: use
  name: d8:use:capability:module:virtualization:forward_port
rules:
- apiGroups:
  - subresources.virtualization.deckhouse.io
  resources:
  - virtualmachines/portforward
  verbs:
  - get
  - create
  - update
