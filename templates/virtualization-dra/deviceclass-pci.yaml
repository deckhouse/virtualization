{{- if eq (include "virtualization-dra.isEnabled" .) "true"}}
apiVersion: resource.k8s.io/v1
kind: DeviceClass
metadata:
  name: pci-devices.virtualization.deckhouse.io
  {{- include "helm_lib_module_labels" (list . (dict "app" "virtualization-dra-pci")) | nindent 2 }}
spec:
  selectors:
    - cel:
        expression: "device.driver == 'virtualization-pci'"
{{- end }}
