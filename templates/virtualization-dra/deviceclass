apiVersion: resource.k8s.io/v1beta1
kind: DeviceClass
metadata:
  name: usb-devices.virtualization.deckhouse.io
spec:
  selectors:
    - cel:
        expression: "device.driver == 'virtualization-usb'"
