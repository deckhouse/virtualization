linters-settings:
  module:
    exclude-rules:
      license:
        directories:
          - tests/
    # CDI patches are soon to be phased out by moving them to 3p repo
    patches:
      disable: true
