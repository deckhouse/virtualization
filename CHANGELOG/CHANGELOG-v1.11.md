# Changelog v1.11

## v1.11.0

### api

- **fix** (default): The inventory generated by `d8 v ansible-inventory` now includes the groups and host variables set for the ansible module. ([!185](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/185))

### ci

- **fix** (low): Go lint jobs no longer fail intermittently when several run on the same CI runner. ([!14](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/14))
- **fix** (low): Pipeline schedules no longer run main-branch build jobs; each schedule runs only its intended jobs. ([!16](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/16))
- **fix** (low): Disable automatic triggers in GitHub Actions workflows; all workflows are now manual-only (workflow_dispatch). ([!20](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/20))
- **fix** (low): Changelog blocks now use bare section names (e.g. `section: ci`), matching the upstream changelog-action format. ([!21](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/21))
- **fix** (low): Accept chore and docs types in the changelog-entry check, matching the upstream changelog action. ([!32](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/32))
- **chore** (low): go.work is the single source of dependency overrides: module images build in workspace mode and CI rejects per-module replace directives, preventing images built with drifted dependencies. ([!40](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/40))
- **fix** (low): The scheduled daily Loop summary of open MRs now runs automatically instead of waiting for a manual start. ([!45](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/45))
- **chore** (low): Emit JUnit reports from Go unit-test jobs so GitLab shows a test-summary widget. ([!78](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/78))
- **fix** (low): Changelog check no longer picks up the template example inside HTML comments. ([!85](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/85))
- **fix** (low): Prod release-channels pipeline can run on any ref (e.g. main) and builds the checked-out release tag, so a broken release pipeline no longer requires re-tagging. ([!91](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/91))
- **fix** (low): Release-channel deploy jobs now authenticate crane against the PROD registry (inherit the Setup werf login), fixing UNAUTHORIZED on crane copy. ([!93](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/93))
- **fix** (low): Release and Svace pipeline notifications are now delivered on failure, not only on success. ([!94](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/94))
- **fix** (low): gitleaks jobs no longer re-run when unrelated earlier-stage jobs are retried ([!95](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/95))
- **fix** (low): Set a 12h timeout for the svace build CI job so long-running Svace-instrumented builds are not killed by the runner. ([!97](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/97))
- **fix** (low): Weekly Svace analysis no longer fails during setup on a registry login error. ([!100](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/100))
- **fix** (low): pin upstream modules-gitlab-ci templates (including the Trivy CVE scan) to v15.0 ([!109](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/109))
- **fix** (low): E2E release-upgrade pipeline waits for VM migrations to finish before the post-upgrade network check, removing flaky failures. ([!116](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/116))
- **fix** (low): Prod build resource_group is scoped per tag and edition so different tags and editions build concurrently instead of serializing on a single global mutex. ([!122](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/122))
- **chore** (low): The release-upgrade e2e pipeline now fails when live-migration VMOPs are never created, instead of silently passing. ([!124](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/124))
- **fix** (low): Restore registry login in the prod release-channel checks (prod:check-requirements and prod:check-version) and align check:registry to registry.deckhouse.io so crane authenticates instead of pulling anonymously. ([!143](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/143))
- **fix** (low): A failed release requirements check now blocks the SE-Plus and FE release channels, not only CE and EE. ([!147](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/147))
- **chore** (low): Return `vladislav.panfilov` as owner of documentation in both CODEOWNERS files. ([!149](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/149))
- **fix** (low): Add Apache 2.0 / Flant JSC license header. ([!169](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/169))
- **chore** (low): Release-upgrade e2e reports the virtualization alerts that fired during the rollover. ([!186](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/186))
- **fix** (low): The GitLab CODEOWNERS file no longer reports validation errors, and changes to tests, CI configuration and build templates reach the right code owners. ([!193](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/193))
- **chore** (low): Cyrillic and documentation validations no longer fail on unrelated files on release branches. ([!198](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/198))
- **chore** (low): Base images are referenced directly instead of being discovered by pattern matching over build files. ([!201](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/201))
- **fix** (low): Run lint:go on release branches instead of failing on the missing go.work. ([!209](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/209))
- **chore** (low): Svace analysis can be dispatched for any tag, branch or commit without rebuilding a release. ([!210](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/210))
- **chore** (low): The no-cyrillic validation skips fuzz tests, whose seed corpus is input data. ([!223](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/223))
- **chore** (low): QEMU and libvirt are built from source in the module again, so the module's own patches are applied. ([!227](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/227))
- **fix** (low): The nightly e2e pipeline disables all migration limits on the virtualization ModuleConfig and runs the suite in parallel. ([!238](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/238))
- **fix** (low): The changelog of a release no longer carries the example from the merge request template instead of the entries their authors wrote. ([!270](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/270))
- **fix** (low): CI jobs no longer fail at the source checkout step because of files left root-owned by the vm-route-forge code generation. ([!278](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/278))

### core

- **fix** (default): The virtual machine migration queue is no longer blocked when one of the migrations ends abnormally. ([!43](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/43))
- **chore** (default): Fixed vulnerabilities:
- CVE-2026-46600
- CVE-2026-56852
- GHSA-hrxh-6v49-42gf
- CVE-2026-34986
- CVE-2025-27144 ([!56](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/56))
- **fix** (low): In-place VM resource resize now works reliably regardless of the feature gate configuration. ([!62](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/62))
- **feature** (default): Added the `Deleting` condition that reports why a resource is being deleted. ([!63](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/63))
- **chore** (low): Local delve debug images build again after the workspace and fox migrations, from arm64 hosts as well; unused mirrord tooling removed. ([!74](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/74))
- **fix** (default): Fixed the loss of access to disks when a USB device is attached to a running virtual machine. ([!79](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/79))
- **feature** (default): Added the ability to find out who is connected to the serial console or VNC session of a virtual machine and to disconnect them. ([!99](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/99))
- **chore** (default): Fixed vulnerabilities:
- GHSA-gcjh-h69q-9w9g ([!101](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/101))
- **fix** (default): The creation of an image or a disk with a long name no longer hangs. ([!102](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/102))
- **fix** (default): Not-ready images and disks no longer hang when deleted while their data is being imported. ([!102](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/102))
- **chore** (default): The image upload server configuration was restructured. ([!104](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/104))
- **fix** (default): Hotplug attachment pods now inherit the priority class of the virtual machine, so they are no longer preempted before the VM itself. ([!105](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/105))
- **fix** (default): USB device passthrough now works on nodes where the usbip driver provides more than one virtual host controller. ([!115](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/115))
- **fix** (default): Fixed the renewal of TLS certificates in the virtualization components while the cluster is degraded. ([!125](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/125))
- **chore** (default): Fixed vulnerabilities:
- CVE-2026-54332
- CVE-2026-54345 ([!146](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/146))
- **feature** (default): Images and disks from an HTTP source can now be verified with SHA-1, SHA-512, and GOST R 34.11-2012 (Streebog) checksums. ([!188](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/188))
- **chore** (default): Fixed vulnerabilities:
- CVE-2026-56864
- CVE-2026-56865
- GO-2026-5932 ([!225](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/225))
- **fix** (default): virt-handler no longer crashes several times a day with a fatal error that interrupted hotplug volume mounts, migrations, and VM status updates. ([!234](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/234))
- **fix** (default): virt-handler no longer crashes several times a day with a fatal error that interrupted hotplug volume mounts, migrations, and VM status updates. ([!235](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/235))
- **chore** (low): The disk image upload and import paths now have fuzz targets, including the PVC importer, and can be fuzzed locally in a container. ([!258](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/258))
- **fix** (low): The disk import pod no longer crash-loops without a message when it gets an unparsable image size. ([!259](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/259))
- **chore** (default): Fixed vulnerabilities:
- CVE-2026-10722 ([!268](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/268))
- **fix** (default): Updated vulnerable dependencies to mitigate known CVEs. ([!275](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/275))
- **fix** (default): The importer no longer panics on a malformed image size in the source. ([!276](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/276))
- **chore** (low): Keep go.work.sum in sync with the workspace module graph. ([!284](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/284))

### cvi

- **fix** (default): During an image upload, the phase of a ClusterVirtualImage no longer flips back to `Pending`. ([!67](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/67))
- **feature** (default): A resources with the `Upload`type that receives no data within 10 minutes moves to the `Failed` phase with the `WaitForUserUploadTimeout` reason. To upload the image, recreate the resource. ([!111](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/111))

### disks

- **fix** (default): Disk import and upload no longer fail with ImagePullBackOff when the module is installed from a registry that differs from the cluster registry. ([!24](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/24))
- **chore** (default): Provisioner pods no longer receive per-namespace copies of the module registry pull secret; the mechanism is replaced by a node-level image prepuller. ([!55](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/55))
- **feature** (default): Image upload for virtual disks can be published through the Gateway API instead of an Ingress. ([!104](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/104))
- **fix** (default): The creation of a virtual disk no longer hangs when the target virtual machine runs on dedicated nodes with taints. ([!121](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/121))

### docs

- **fix** (default): Broken alert in user guide has been fixed. ([!106](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/106))

### images

- **feature** (default): Image upload for virtual images can be published through the Gateway API instead of an Ingress. ([!104](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/104))
- **feature** (default): The module's runtime images have been migrated to a distroless base, which reduces the size of the virtualization image by about 91 MB. ([!151](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/151))

### module

- **feature** (low): Go modules are unified under a single go.work workspace with aligned dependencies. ([!3](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/3))
- **chore** (low): Added the threat model of the module for FSTEC certification services. ([!41](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/41))
- **chore** (default): All Go modules resolve dependencies through a single workspace, so images can no longer be built with stale or vulnerable library versions pinned in forgotten per-module files. ([!68](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/68))
- **fix** (default): Fixed device handling for virtual machines on cgroup v2 nodes. ([!96](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/96))
- **feature** (default): New settings to select the Gateway API gateway and the hostname used to publish image upload. ([!104](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/104))
- **feature** (default): USB devices are provided only from nodes where usbip kernel modules are actually available. ([!145](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/145))
- **chore** (default): Add a Links section (Kaiten card, ADR, KubeVirt fork MR) to the MR and PR templates. ([!148](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/148))
- **fix** (default): The Deckhouse queue is no longer blocked when the `virtualization` module is enabled while its controllers are not ready. ([!218](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/218))
- **fix** (default): Node configuration no longer fails on nodes where the usbip kernel modules cannot be installed. ([!219](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/219))
- **fix** (default): Node configuration no longer fails on nodes where usbip kernel modules are unavailable. ([!274](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/274))

### observability

- **feature** (default): Added alerts for unavailable USB passthrough and for required module components missing on some nodes. ([!171](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/171))
- **fix** (default): Fixed the module's alerts suppressing the platform alert about unavailable components. ([!171](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/171))
- **chore** (default): Alerts now point to the component that failed; a stuck controller queue and a missing leader are tracked separately. ([!175](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/175))
- **feature** (default): Added metrics export for VirtualMachineClass. ([!179](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/179))
- **feature** (default): Added a metric that reports whether a virtual machine can be live migrated. ([!181](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/181))
- **fix** (default): Removed the metric that reported virtual machines with local disks as impossible to migrate: such machines can be migrated. ([!181](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/181))

### test

- **chore** (low): The SDN e2e precheck now prints a copy-paste-ready ClusterNetwork manifest with the IPAM pool bound for IPAM tests. ([!76](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/76))
- **chore** (low): Drop the e2e clone-size workaround for sds-local-volume. ([!98](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/98))
- **fix** (low): Release upgrade smoke test no longer panics while validating iperf network continuity across the firmware-update migration. ([!110](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/110))
- **fix** (low): Release upgrade e2e test now fails if a VM migration triggered by the upgrade did not complete successfully. ([!118](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/118))
- **fix** (low): Enable the ingress-nginx module explicitly in the DVP static cluster config, since it is no longer an embedded Deckhouse module. ([!162](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/162))
- **fix** (low): The release-upgrade e2e suite now fails if a virtual machine restarts instead of staying up during a module upgrade. ([!199](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/199))
- **chore** (low): Added a seed corpus for the DVCR uploader fuzz test. ([!223](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/223))
- **fix** (low): Nested cluster bootstrap in e2e no longer fails on a single network hiccup while talking to the parent cluster. ([!263](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/263))
- **chore** (low): Nodes of the nested e2e cluster advertise a TSC frequency, so Windows-osType virtual machines can run there. ([!280](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/280))

### vd

- **fix** (default): Fixed the restore of a virtual disk from a snapshot on storage that rounds volume sizes up (`ceph-rbd`, `sds-elastic`). ([!48](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/48))
- **feature** (default): Creating a virtual disk from a raw image in the registry onto a block device no longer makes an extra copy of the data. ([!65](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/65))
- **feature** (default): Disk and image import is faster. ([!86](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/86))
- **feature** (default): A resources with the `Upload`type that receives no data within 10 minutes moves to the `Failed` phase with the `WaitForUserUploadTimeout` reason. To upload the image, recreate the resource. ([!111](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/111))
- **fix** (default): Disk migration is no longer reverted because of a parallel migration of another virtual machine or a short readiness flap at migration switchover. ([!189](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/189))
- **fix** (default): Fixed the import of virtual disks failing on nodes that require containers to run with a read-only root filesystem. ([!240](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/240))
- **fix** (default): Fixed import from a PVC source failing because the read-only pvc-importer image lacked the /source mount point. ([!273](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/273))

### vi

- **feature** (default): A resources with the `Upload`type that receives no data within 10 minutes moves to the `Failed` phase with the `WaitForUserUploadTimeout` reason. To upload the image, recreate the resource. ([!111](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/111))

### vm

- **fix** (default): The controller no longer endlessly recreates the internal resource claim template for virtual machines with attached USB devices. ([!6](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/6))
- **feature** (default): Added the ability to attach physical GPUs to virtual machines. The functionality is experimental and available in the EE/SE+ commercial editions. To enable it, add `GPU` to `.spec.settings.featureGates` in the ModuleConfig of the `virtualization` module; the cluster must run Kubernetes `1.34` or newer. ([!7](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/7))
- **feature** (default): Added the `Auto` value for `.spec.cpu.coreFraction`. The functionality is available in the EE commercial edition. To enable it, add `VerticalVirtualMachineAutoscaler` and `HotplugCPUAndMemoryWithInPlaceResize` to `.spec.settings.featureGates` in the ModuleConfig of the `virtualization` module. ([!8](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/8))
- **fix** (low): Virtual machines with hotplug disks no longer briefly report a failed-to-start error while starting. ([!34](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/34))
- **fix** (default): The live migration of a virtual machine with additional networks no longer hangs waiting for the network on the target node. ([!52](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/52))
- **feature** (default): The cloud-init configuration can now be detached from a virtual machine without restarting the machine. ([!77](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/77))
- **feature** (default): Malformed cloud-init data is now reported as a warning when the resource is created, and in the `ProvisioningReady` condition of an existing resource. ([!107](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/107))
- **fix** (default): Fixed a not-ready image blocking the attachment and detachment of a virtual machine's disks. ([!113](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/113))
- **chore** (default): More informative message when no CIDRs specified in ModuleConfig and SDN is required. ([!114](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/114))
- **feature** (default): Added the `Legacy` value for `.spec.osType` to support guest operating systems without built-in `AHCI` and `virtio` drivers, such as Windows XP and Windows Server 2003. ([!126](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/126))
- **fix** (default): Fixed a crash loop of a virtual machine with an additional network interface. ([!132](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/132))
- **fix** (default): The service components of a virtual machine with Secure Boot no longer hang during its migration. ([!136](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/136))
- **fix** (default): Fixed image mounts leaking on the target node after a live migration of a virtual machine. ([!137](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/137))
- **fix** (default): The switch from in-place CPU resize to live migration is reported once instead of on every reconcile. ([!141](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/141))
- **fix** (default): Detaching a network interface from `.spec.networks` no longer leaves a stale interface in the virtual machine's pod. ([!150](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/150))
- **fix** (default): A virtual machine no longer starts booting before its boot disk is attached. ([!158](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/158))
- **fix** (default): Image upload can now be published through the Gateway API instead of an Ingress. This requires the `alb` module; to enable it, add `UploadViaAPIGateway` to `.spec.settings.featureGates` in the ModuleConfig of the `virtualization` module. ([!184](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/184))
- **fix** (default): A virtual machine with an attached USB device can now be live migrated and evacuated during a node drain. ([!187](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/187))
- **fix** (default): The virtualization controller no longer crashloops on clusters running Kubernetes older than 1.34. ([!190](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/190))
- **fix** (low): The VirtualMachineClass CRD applies on Kubernetes 1.33 again; a class is now limited to 16 sizing policies. ([!216](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/216))
- **fix** (default): Canceling a disk migration no longer loses the data of the virtual machine. ([!228](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/228))
- **fix** (default): An additional network attached to a VirtualMachine no longer stays unconfigured when another network has a pinned MAC address. ([!230](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/230))
- **fix** (default): An additional network attached to a VirtualMachine no longer stays unconfigured when another network has a pinned MAC address. ([!237](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/237))
- **fix** (default): Canceling a disk migration no longer loses the data of the virtual machine. ([!245](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/245))

### vmop

- **feature** (default): A live migration queued behind concurrency limits now reports which limit is blocking it. ([!5](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/5))
- **fix** (default): Fixed the force stop of a virtual machine when a regular stop was already in progress. ([!22](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/22))
