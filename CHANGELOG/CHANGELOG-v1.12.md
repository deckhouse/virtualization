# Changelog v1.12

## v1.12.0

### api

- **fix** (default): A VirtualMachineOperation of the `Evict` type no longer hangs in `Terminating` after its virtual machine is deleted. ([!239](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/239))
- **fix** (low): The list of virtual machine pods in the status keeps a stable order, so the status is no longer rewritten on every reconcile. ([!367](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/367))

### ci

- **feature** (low): Added CI builds for dvcr-artifact and pvc-artifact fuzz images. ([!264](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/264))
- **chore** (low): Rework the svace build template: build once, support per-image svace options, import settings and ignore rules, and run the temp_pod build with --enable-ptrace-all. ([!286](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/286))
- **chore** (low): Nightly e2e storage setup collects the sds-elastic module state when it times out. ([!300](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/300))
- **fix** (low): Nightly e2e storage setup no longer times out when rerun on a cluster whose disks are already in use. ([!301](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/301))
- **chore** (low): Merge request titles are checked against the commit message convention. ([!305](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/305))
- **fix** (low): Nested-cluster bootstrap survives a StaticInstance that caps stopped driving. ([!306](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/306))
- **fix** (low): A manually started pipeline is no longer cancelled by unrelated pushes to main. ([!314](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/314))
- **fix** (low): Release e2e tests no longer fail before they start because of the migration limits precheck. ([!316](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/316))
- **fix** (low): The fuzz image build no longer runs on every merge request pipeline. ([!320](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/320))
- **chore** (low): Update QEMU to 10.2.4 and align QEMU patches with the new source version. ([!336](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/336))
- **feature** (low): The e2e suite can be run on demand from an MR pipeline against a leased pool cluster, without bootstrapping one. ([!339](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/339))
- **chore** (low): nightly cluster keep hours set to 23 ([!355](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/355))
- **fix** (low): Nightly e2e on sds-elastic no longer times out on the Ceph startup crash warning. ([!405](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/405))
- **chore** (low): Fix the nightly svace scan of the spice-server image. ([!416](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/416))
- **chore** (low): Fixed e2e image digest verification getting stuck when PCI DRA is disabled. ([!434](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/434))
- **chore** (low): Stabilize gofumpt formatting of the changelog parser test to stop flaky lint:go failures. ([!444](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/444))
- **fix** (low): The Go lint job runs a pinned linter image, so its verdict no longer depends on the runner. ([!468](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/468))

### cli

- **fix** (low): An inventory generated by d8 v ansible-inventory keeps the cluster it was generated for, instead of falling back to the current kubeconfig context. ([!292](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/292))

### core

- **fix** (default): Deleting a virtual machine after its live migration on NFS no longer leaves the machine, its disks and the namespace in `Terminating`. ([!242](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/242))
- **fix** (low): The USB DRA driver no longer crash-loops on a node when a resource claim carries an unexpected device name. ([!261](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/261))
- **feature** (default): Added the ability to attach PCI devices of cluster nodes to virtual machines. The functionality is available in the commercial editions of the platform and is experimental. To use it, the cluster must run Kubernetes 1.34 or newer. ([!262](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/262))
- **fix** (low): The virtual machine pod image no longer ships two versions of the same library, and is 15 MB smaller. ([!285](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/285))
- **fix** (default): Virtual machine pods now run with the RuntimeDefault seccomp profile. A running machine gets it after a restart or a migration. ([!324](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/324))
- **chore** (default): Fixed vulnerabilities:
- CVE-2026-56854 ([!326](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/326))
- **chore** (low): Source the containerized-data-importer fork from fox.flant.com instead of the GitHub mirror. ([!337](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/337))
- **chore** (low): Clone the containerized-data-importer fork from fox.flant.com instead of the SOURCE_REPO mirror. ([!340](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/340))
- **chore** (low): Bumped golang.org/x/crypto to v0.55.0 and google.golang.org/grpc to v1.83.1 in the module images to fix CVE-2026-56854 and CVE-2026-84304. ([!346](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/346))
- **fix** (low): Virtual machines no longer fail to start on some nodes depending on the node's container runtime. ([!356](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/356))
- **fix** (default): Automatic rebalancing no longer tries to evict virtual machines that cannot be live migrated. ([!368](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/368))
- **fix** (low): The descheduler annotations and the live-migratable label set on a virtual machine are no longer propagated to its pod. ([!368](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/368))
- **chore** (default): Fixed vulnerabilities:
- CVE-2026-84304 ([!369](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/369))
- **fix** (default): Module-reserved labels and annotations are no longer propagated from user resources onto internal objects. ([!380](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/380))
- **chore** (default): Fixed vulnerabilities:
- CVE-2026-84445 ([!417](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/417))
- **chore** (low): Fix fuzzer finding: conntrack sync message may lead to memory over-allocation. ([!422](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/422))
- **fix** (low): The virtualization controller no longer floods its log with change-detection records on every reconcile. ([!466](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/466))

### disks

- **fix** (low): Disk import from an image is several times faster because data is written in full 1 MiB blocks instead of short network-sized pieces. ([!265](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/265))
- **fix** (default): An interrupted image download now fails the import. ([!265](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/265))
- **fix** (low): Empty parts of an imported disk are cleared by the storage instead of being written out byte by byte. ([!266](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/266))
- **fix** (low): The controller can delete its own VolumeSnapshots when the state-snapshotter module is enabled. ([!283](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/283))
- **fix** (default): Image and disk import and upload now work in Deckhouse projects with an isolated network. ([!307](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/307))
- **fix** (default): An interrupted disk import now completes on the next attempt. ([!310](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/310))
- **fix** (default): A virtual machine can now be restored and cloned from a snapshot even if the default storage class of the cluster uses another CSI driver. ([!392](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/392))
- **fix** (default): A virtual machine restored from a snapshot on local storage no longer hangs in `Pending`. All of its volumes now end up on the same node. ([!400](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/400))

### docs

- **docs** (low): The module's security threat model is updated to the current state of the module. ([!254](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/254))
- **docs** (low): Added release notes v1.10.4. ([!317](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/317))
- **docs** (low): Documentation updates. ([!341](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/341))
- **docs** (low): Documentation fixes. ([!397](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/397))
- **docs** (low): Revert release notes v1.12.0. ([!437](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/437))
- **docs** (low): Added release notes v1.12.0. ([!438](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/438))
- **docs** (low): Update the documentation and release notes for v1.12.0 to match the available functionality. ([!470](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/470))

### dvcr

- **chore** (low): The container registry image is built from the pinned distribution fork instead of a patched upstream checkout. ([!345](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/345))

### images

- **feature** (default): Disks are created from images faster, and a raw image takes several times less space because it is now stored compressed. ([!266](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/266))

### module

- **docs** (low): Release notes follow the platform-wide format. ([!217](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/217))
- **chore** (low): Settings containing YAML-significant characters no longer alter or break the manifests the module generates. ([!294](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/294))
- **fix** (default): PCI devices are now available in clusters with the legacy user-authz access model, the same way as USB devices. ([!431](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/431))

### observability

- **feature** (default): The dashboards now show how many virtual machines cannot migrate and which nodes they run on. ([!287](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/287))
- **feature** (default): Added the d8_virtualization_virtualmachine_eviction_required metric about the state of a virtual machine while its node is taken out of service. The D8VirtualizationVirtualMachineHoldsNodeMaintenance alert names a virtual machine that holds such a node. ([!291](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/291))
- **fix** (default): The D8VirtualizationNodeEvacuationStuck alert no longer fires while a live migration of a virtual machine is running. ([!291](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/291))
- **feature** (default): The d8_virtualization_virtualmachine_migratable metric gained the reason label and is no longer dropped to zero by a cordon of a neighbouring node, so alerts matching on its value alone change their meaning. ([!297](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/297))
- **feature** (default): Metrics now tell which node a virtual machine migrates from and which node it migrates to, and the dashboards show how the migrations in progress are going. ([!330](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/330))
- **feature** (low): The dashboard tells whether a migration in progress is converging, whether it moves the disks of the machine along with its memory, and how much migration traffic each node carries. ([!330](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/330))
- **chore** (default): Four migration metrics inherited from KubeVirt moved under the d8_internal_virtualization_kubevirt_ prefix. Dashboards and alerts that use them require an update, and the same data is now reported by the d8_virtualization_virtualmachine_migration_info metric. ([!330](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/330))
- **feature** (default): Added metrics of the launch, shutdown and migration time of a virtual machine and of the disk provisioning time. ([!354](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/354))
- **feature** (default): The cluster overview dashboard now also shows the actual CPU, memory and disk space consumption of virtual machines. ([!365](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/365))
- **fix** (default): The CPU utilization panel of the cluster overview dashboard no longer reports values about a thousand times lower than the real ones. ([!365](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/365))
- **fix** (default): The cluster overview dashboard opens with the default Prometheus data source. ([!365](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/365))
- **fix** (low): Import and upload Pods now report a metrics port that could not be bound instead of running without metrics silently. ([!384](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/384))

### test

- **chore** (low): Add an e2e spec for a virtual machine created with paravirtualization disabled. ([!311](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/311))
- **chore** (low): Fix a false failure in the release upgrade smoke test caused by the iperf3 client's own stop signal. ([!321](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/321))
- **fix** (low): The e2e teardown no longer returns a cluster to the shared pool as reusable while it still holds test objects or an unusable platform. ([!376](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/376))
- **chore** (low): Cloud-init user data, the USB/IP protocol and the importer metrics endpoint are covered by fuzz targets. ([!384](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/384))

### vd

- **fix** (default): Restoring a disk with a size of zero or less is now rejected with a clear reason. ([!166](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/166))
- **fix** (low): The data of a virtual disk is no longer lost when its migration is finalized while the disk is still in use. ([!229](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/229))
- **fix** (low): Fixed the import of virtual disks failing on nodes that require containers to run with a read-only root filesystem. ([!231](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/231))
- **fix** (low): Fixed import from a PVC source failing because the read-only pvc-importer image lacked the /source mount point. ([!277](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/277))
- **fix** (low): VirtualDisk upload and progress reporting now work in network-isolated Deckhouse Projects. ([!307](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/307))
- **fix** (default): A large image import no longer loops because the importer runs out of memory. ([!312](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/312))
- **fix** (default): A failed disk or image import now names the reason instead of staying in `Provisioning` forever. ([!312](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/312))
- **fix** (low): An import that fails while writing the image to storage no longer reports success. ([!312](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/312))
- **fix** (low): PVC volume and access modes can no longer be overridden via VirtualDisk/VirtualImage annotations; they are read only from the StorageClass. ([!349](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/349))
- **fix** (low): A virtual machine now boots from a disk created from a gzip- or xz-compressed image. ([!386](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/386))

### vdsnapshot

- **feature** (low): Support snapshot archives download and restore via `d8 snapshot download|upload` commands. ([!407](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/407))

### vi

- **fix** (low): VirtualImage u now work innetwork-isolated Deckhouse Projects. ([!307](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/307))
- **fix** (low): An image stored in a PersistentVolumeClaim now reports why its import failed instead of staying in Provisioning. ([!312](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/312))
- **fix** (low): An image whose import failed returns to Ready by itself once the cause is gone. ([!312](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/312))
- **fix** (default): An image import blocked by a project quota resumes by itself once the quota is raised. ([!312](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/312))

### vm

- **chore** (low): Refactor VM and VD snapshot to use unified snapshotter machinery for snapshots instead of storing resources in Secrets. ([!61](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/61))
- **feature** (default): Added the SPICE display, which gives a virtual machine the guest sound, USB redirection from the client and a shared clipboard. ([!157](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/157))
- **feature** (default): VNC now uses less traffic thanks to a better compression of the picture. ([!157](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/157))
- **feature** (low): A virtual machine can be restored and cloned from a snapshot taken by the unified snapshot mechanism. ([!166](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/166))
- **feature** (default): New snapshots of virtual machines and disks are now taken through the `state-snapshotter` module if it is enabled. ([!166](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/166))
- **fix** (low): A virtual machine snapshot no longer reports itself as inconsistent after a failed capture. ([!166](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/166))
- **feature** (low): Virtual machines and their disks are now included in namespace-wide snapshots and can be restored from them (d8 snapshot create, d8 snapshot restore). ([!250](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/250))
- **fix** (default): An additional network is now unplugged from the guest as soon as it is removed from a running virtual machine. ([!256](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/256))
- **fix** (default): A virtual machine whose additional network changed its MAC address during creation now starts with all its interfaces. ([!256](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/256))
- **fix** (default): A virtual machine on an unresponsive node now reports its state as unknown. ([!282](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/282))
- **feature** (default): The Migratable condition now reports that a virtual machine has no node to migrate to. ([!287](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/287))
- **fix** (low): A stopped virtual machine no longer reports the migratability of its previous run. ([!287](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/287))
- **feature** (default): A node can now be freed by allowing the restart of the virtual machines that cannot be live migrated. ([!291](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/291))
- **fix** (low): An evacuation that failed is retried after a backoff instead of being dropped, so a machine no longer keeps a condition promising a migration nobody attempts. ([!291](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/291))
- **feature** (low): A virtual machine stays migratable while the nodes it can move to are temporarily unavailable, and names that as the reason. ([!297](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/297))
- **feature** (low): A virtual machine with hotplug disks on SDS storage now starts on a node with enough space in its storage pools, instead of hanging with disks that cannot attach. ([!304](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/304))
- **fix** (default): A node no longer waits for a reboot because of a virtual machine pod left on it by a live migration. ([!322](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/322))
- **feature** (default): Added the OperationInProgress condition, which reports the operation being performed on a virtual machine and the failure of the last one. ([!325](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/325))
- **fix** (low): Migration attempts against a VirtualMachine with a block device corrupted by a previously failed migration now report a clear reason with a restart recommendation instead of a generic failure. ([!333](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/333))
- **fix** (low): Automatic workload updates (firmware, node placement, hotplug resources) retry a failed live migration up to 3 times instead of silently giving up after the first failure. ([!333](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/333))
- **fix** (default): A virtual machine with local disks is no longer reported as non-migratable if its disks can move along with it. ([!338](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/338))
- **fix** (default): A virtual machine is no longer left pinned to the node where its local disks used to live. ([!338](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/338))
- **fix** (default): A virtual machine whose disk is available on a single node only is no longer reported as migratable. ([!338](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/338))
- **fix** (default): The provisioning secret cannot be deleted while a running virtual machine still needs it. ([!342](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/342))
- **fix** (low): A virtual machine with a hot-plugged disk no longer loses the ability to live migrate. ([!366](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/366))
- **feature** (default): Added the ability to give a virtual machine an emulated Input/Output Memory Management Unit (IOMMU), so that PCI passthrough works inside the guest. The functionality is available in the commercial editions of the platform. To enable it, add VIOMMU to .spec.settings.featureGates in the ModuleConfig of the virtualization module. ([!375](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/375))
- **feature** (default): Live migration now reports how long the virtual machine was actually paused. ([!382](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/382))
- **fix** (low): Cloud-init validation warnings no longer echo the user data back, so the content of a provisioning Secret cannot reach the virtual machine status and Events, and a long payload cannot make the message oversized. ([!409](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/409))
- **fix** (low): Condition messages of a virtual machine are cut to the 32768 bytes the API allows, so a long user-controlled text can no longer block status updates. ([!409](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/409))

### vmclass

- **fix** (default): The module no longer stops working because of a VirtualMachineClass created before its installation finished. ([!457](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/457))

### vmop

- **fix** (default): A migration now reports at once that it has no suitable node to move the virtual machine to. ([!299](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/299))
- **fix** (low): The event of a failed migration names the cause instead of the generic wording, so the events no longer point at the shutdown of the target pod. ([!299](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/299))

### vmsnapshot

- **feature** (low): Support snapshot archives download and restore via `d8 snapshot download|upload` commands. ([!407](https://fox.flant.com/deckhouse/virtualization/virtualization/-/merge_requests/407))
