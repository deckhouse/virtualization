apiVersion: v1
kind: Service
metadata:
  name: nfs-server
  namespace: {{ .Values.namespace }}
  labels:
    app: nfs-server
spec:
  selector:
    app: nfs-server
  ports:
    - name: nfs
      port: 2049
      protocol: TCP
      targetPort: 2049

