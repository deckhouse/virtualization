---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  labels:
    heritage: deckhouse
    module: virtualization
  name: virtualmachinesnapshotoperations.virtualization.deckhouse.io
spec:
  group: virtualization.deckhouse.io
  names:
    categories:
      - virtualization
    kind: VirtualMachineSnapshotOperation
    listKind: VirtualMachineSnapshotOperationList
    plural: virtualmachinesnapshotoperations
    shortNames:
      - vmsop
    singular: virtualmachinesnapshotoperation
  scope: Namespaced
  versions:
    - additionalPrinterColumns:
        - description: VirtualMachineSnapshotOperation phase.
          jsonPath: .status.phase
          name: Phase
          type: string
        - description: VirtualMachineSnapshotOperation type.
          jsonPath: .spec.type
          name: Type
          type: string
        - description: VirtualMachineSnapshot name.
          jsonPath: .spec.virtualMachineSnapshotName
          name: VirtualMachineSnapshot
          type: string
        - description: Time of resource creation.
          jsonPath: .metadata.creationTimestamp
          name: Age
          type: date
      name: v1alpha2
      schema:
        openAPIV3Schema:
          description:
            VirtualMachineSnapshotOperation enables declarative management
            of virtual machine snapshot state changes.
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              properties:
                createVirtualMachine:
                  description: CreateVirtualMachine defines the clone operation.
                  properties:
                    customization:
                      description: Customization defines customization options for cloning.
                      properties:
                        namePrefix:
                          description: |-
                            NamePrefix adds a prefix to resource names during cloning.
                            Applied to VirtualMachine, VirtualDisk, VirtualMachineBlockDeviceAttachment, and Secret resources.
                          type: string
                        nameSuffix:
                          description: |-
                            NameSuffix adds a suffix to resource names during cloning.
                            Applied to VirtualMachine, VirtualDisk, VirtualMachineBlockDeviceAttachment, and Secret resources.
                          type: string
                      type: object
                      x-kubernetes-validations:
                        - message:
                            namePrefix length must be between 1 and 59 characters
                            if set
                          rule:
                            "!has(self.namePrefix) || (size(self.namePrefix) >= 1
                            && size(self.namePrefix) <= 59)"
                        - message:
                            nameSuffix length must be between 1 and 59 characters
                            if set
                          rule:
                            "!has(self.nameSuffix) || (size(self.nameSuffix) >= 1
                            && size(self.nameSuffix) <= 59)"
                    mode:
                      description: |-
                        VMSOPCreateVirtualMachineMode defines the kind of the clone operation.
                        * `Strict`: Strict clone as is in the snapshot.
                        * `BestEffort`: BestEffort clone without deleted external missing dependencies.
                      enum:
                        - DryRun
                        - Strict
                        - BestEffort
                      type: string
                    nameReplacement:
                      description:
                        NameReplacement defines rules for renaming resources
                        during cloning.
                      items:
                        description:
                          NameReplacement represents a rule for redefining
                          the virtual machine resource names.
                        properties:
                          from:
                            description: Selector to choose resources for name replacement.
                            properties:
                              kind:
                                description: Kind of a resource to rename.
                                type: string
                              name:
                                description: Current name of a resource to rename.
                                type: string
                            required:
                              - name
                            type: object
                          to:
                            description: New resource name.
                            type: string
                        required:
                          - from
                          - to
                        type: object
                      type: array
                      x-kubernetes-validations:
                        - message: Each nameReplacement.to must be between 1 and 59 characters
                          rule:
                            self.all(nr, has(nr.to) && size(nr.to) >= 1 && size(nr.to)
                            <= 59)
                  required:
                    - mode
                  type: object
                  x-kubernetes-validations:
                    - message:
                        At least one of customization.namePrefix, customization.nameSuffix,
                        or nameReplacement must be set
                      rule:
                        (has(self.customization) && ((has(self.customization.namePrefix)
                        && size(self.customization.namePrefix) > 0) || (has(self.customization.nameSuffix)
                        && size(self.customization.nameSuffix) > 0))) || (has(self.nameReplacement)
                        && size(self.nameReplacement) > 0)
                type:
                  description: |-
                    Type of the operation to execute on a virtual machine:
                    * `CreateVirtualMachine`: CreateVirtualMachine the virtual machine to a new virtual machine.
                  enum:
                    - CreateVirtualMachine
                  type: string
                virtualMachineSnapshotName:
                  description:
                    Name of the virtual machine snapshot the operation is
                    performed for.
                  type: string
              required:
                - type
                - virtualMachineSnapshotName
              type: object
              x-kubernetes-validations:
                - message: .spec is immutable
                  rule: self == oldSelf
                - message: CreateVirtualMachineName requires clone field.
                  rule:
                    "self.type == 'CreateVirtualMachineName' ? has(self.createVirtualMachine)
                    : true"
            status:
              properties:
                conditions:
                  description:
                    The latest detailed observations of the VirtualMachineSnapshotOperation
                    resource.
                  items:
                    description:
                      Condition contains details for one aspect of the current
                      state of this API Resource.
                    properties:
                      lastTransitionTime:
                        description: |-
                          lastTransitionTime is the last time the condition transitioned from one status to another.
                          This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                        format: date-time
                        type: string
                      message:
                        description: |-
                          message is a human readable message indicating details about the transition.
                          This may be an empty string.
                        maxLength: 32768
                        type: string
                      observedGeneration:
                        description: |-
                          observedGeneration represents the .metadata.generation that the condition was set based upon.
                          For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                          with respect to the current state of the instance.
                        format: int64
                        minimum: 0
                        type: integer
                      reason:
                        description: |-
                          reason contains a programmatic identifier indicating the reason for the condition's last transition.
                          Producers of specific condition types may define expected values and meanings for this field,
                          and whether the values are considered a guaranteed API.
                          The value should be a CamelCase string.
                          This field may not be empty.
                        maxLength: 1024
                        minLength: 1
                        pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                        type: string
                      status:
                        description: status of the condition, one of True, False, Unknown.
                        enum:
                          - "True"
                          - "False"
                          - Unknown
                        type: string
                      type:
                        description: type of condition in CamelCase or in foo.example.com/CamelCase.
                        maxLength: 316
                        pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                        type: string
                    required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                    type: object
                  type: array
                observedGeneration:
                  description: " Resource generation last processed by the controller."
                  format: int64
                  type: integer
                phase:
                  description: |-
                    Current phase of the resource:
                    * `Pending`: The operation is queued for execution.
                    * `InProgress`: The operation is in progress.
                    * `Completed`: The operation has been completed successfully.
                    * `Failed`: The operation failed. For details, refer to the `conditions` field and events.
                    * `Terminating`: The operation is being deleted.
                  enum:
                    - Pending
                    - InProgress
                    - Completed
                    - Failed
                    - Terminating
                  type: string
                resources:
                  description:
                    Resources contains the list of resources that are affected
                    by the snapshot operation.
                  items:
                    description:
                      SnapshotResourceStatus defines the resource affected
                      by the operation.
                    properties:
                      apiVersion:
                        description: API version of the resource.
                        type: string
                      kind:
                        description: Kind of the resource.
                        type: string
                      message:
                        description: Message about the resource.
                        type: string
                      name:
                        description: Name of the resource.
                        type: string
                      status:
                        description: Status of the resource.
                        enum:
                          - InProgress
                          - Completed
                          - Failed
                        type: string
                    required:
                      - apiVersion
                      - kind
                      - message
                      - name
                      - status
                    type: object
                  type: array
              required:
                - phase
              type: object
          required:
            - spec
          type: object
      served: true
      storage: true
      subresources:
        status: {}
