---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  labels:
    heritage: deckhouse
    module: virtualization
  name: virtualmachinerestores.virtualization.deckhouse.io
spec:
  group: virtualization.deckhouse.io
  names:
    categories:
      - virtualization
    kind: VirtualMachineRestore
    listKind: VirtualMachineRestoreList
    plural: virtualmachinerestores
    shortNames:
      - vmrestore
      - vmrestores
    singular: virtualmachinerestore
  scope: Namespaced
  versions:
    - additionalPrinterColumns:
        - description: VirtualMachineRestore phase.
          jsonPath: .status.phase
          name: Phase
          type: string
        - description: VirtualMachineRestore age.
          jsonPath: .metadata.creationTimestamp
          name: Age
          type: date
      name: v1alpha2
      schema:
        openAPIV3Schema:
          description:
            Provides a resource for restoring virtual machines from snapshots.
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                Must be written in CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              properties:
                nameReplacements:
                  description: Renaming conventions for virtual machine resources.
                  items:
                    description:
                      Represents a rule for redefining the virtual machine resource names.
                    properties:
                      from:
                        description: Selector to choose resources for renaming.
                        properties:
                          kind:
                            description: Kind of a resource to rename.
                            type: string
                          name:
                            description: Name of a resource to rename.
                            type: string
                        required:
                          - name
                        type: object
                      to:
                        description: New resource name.
                        type: string
                    required:
                      - from
                      - to
                    type: object
                  type: array
                virtualMachineSnapshotName:
                  description:
                    Snapshot name to restore a virtual machine.
                  minLength: 1
                  type: string
              required:
                - virtualMachineSnapshotName
              type: object
            status:
              properties:
                conditions:
                  description: Last confirmed status of the resource.
                  items:
                    description:
                      Details of a single aspect of the current
                      state for this API resource.
                    properties:
                      lastTransitionTime:
                        description: |-
                          Last time the condition transitioned from one status to another.
                        format: date-time
                        type: string
                      message:
                        description: |-
                          Human-readable message with details about the last transition.
                        maxLength: 32768
                        type: string
                      observedGeneration:
                        description: |-
                          `.metadata.generation` the condition was based upon.
                          For instance, if `.metadata.generation` is currently `12` but `.status.conditions[x].observedGeneration` is `9`, the condition is considered outdated.
                        format: int64
                        minimum: 0
                        type: integer
                      reason:
                        description: |-
                          Brief reason for the condition's last transition.
                        maxLength: 1024
                        minLength: 1
                        pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                        type: string
                      status:
                        description: |-
                          Condition status. Possible values: `True`, `False`, or `Unknown`.
                        enum:
                          - "True"
                          - "False"
                          - Unknown
                        type: string
                      type:
                        description: Condition type.
                        maxLength: 316
                        pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                        type: string
                    required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                    type: object
                  type: array
                observedGeneration:
                  description: Resource generation last processed by the controller.
                  format: int64
                  type: integer
                phase:
                  description: |-
                    Current status of the VirtualMachineRestore resource:

                    * `Pending`: The resource has been created and is on a waiting queue.
                    * `InProgress`: A virtual machine is being restored from a snapshot.
                    * `Ready`: A virtual machine has been successfully restored from a snapshot.
                    * `Failed`: An error occurred when restoring a virtual machine from a snapshot.
                    * `Terminating`: The resource is being deleted.
                  enum:
                    - Pending
                    - InProgress
                    - Ready
                    - Failed
                    - Terminating
                  type: string
              required:
                - phase
              type: object
          required:
            - spec
          type: object
      served: true
      storage: true
      subresources:
        status: {}
