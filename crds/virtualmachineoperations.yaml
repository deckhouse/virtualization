---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  labels:
    heritage: deckhouse
    module: virtualization
  name: virtualmachineoperations.virtualization.deckhouse.io
spec:
  group: virtualization.deckhouse.io
  names:
    categories:
      - virtualization
      - all
    kind: VirtualMachineOperation
    listKind: VirtualMachineOperationList
    plural: virtualmachineoperations
    shortNames:
      - vmop
      - vmops
    singular: virtualmachineoperation
  scope: Namespaced
  versions:
    - additionalPrinterColumns:
        - description: VirtualMachineOperation phase.
          jsonPath: .status.phase
          name: Phase
          type: string
        - description: VirtualMachineOperation type.
          jsonPath: .spec.type
          name: Type
          type: string
        - description: VirtualMachine name.
          jsonPath: .spec.virtualMachineName
          name: VirtualMachine
          type: string
        - description: Time of the resource creation.
          jsonPath: .metadata.creationTimestamp
          name: Age
          type: date
      name: v1alpha2
      schema:
        openAPIV3Schema:
          description:
            Enables declarative management of virtual machine state changes.
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                Must be written in CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              properties:
                force:
                  description:
                    Force execution of an operation. Applies only to the
                    `Restart` and `Stop` operations. In this case, the operation on a virtual machine
                    is performed immediately.
                  type: boolean
                type:
                  description: |-
                    Type of the operation to execute on a virtual machine:

                    * `Start`: Start a virtual machine.
                    * `Stop`: Stop a virtual machine.
                    * `Restart`: Restart a virtual machine.
                    * `Migrate` (deprecated value): Migrate a virtual machine to another node where it can be started.
                    * `Evict` - Migrate a virtual machine to another node where it can be started.
                  enum:
                    - Restart
                    - Start
                    - Stop
                    - Migrate
                    - Evict
                  type: string
                virtualMachineName:
                  description:
                    Virtual machine name the operation is performed for.
                  type: string
              required:
                - type
                - virtualMachineName
              type: object
              x-kubernetes-validations:
                - message: The `Start` operation cannot be performed forcibly.
                  rule: "self.type == 'Start' ? !has(self.force) || !self.force : true"
                - message: The `Migrate` operation cannot be performed forcibly.
                  rule:
                    "self.type == 'Migrate' ? !has(self.force) || !self.force :
                    true"
            status:
              properties:
                conditions:
                  description:
                    Last confirmed status of the resource.
                  items:
                    description:
                      Details of a single aspect of the current state for this API resource.
                    properties:
                      lastTransitionTime:
                        description: |-
                          Last time the condition transitioned from one status to another.
                        format: date-time
                        type: string
                      message:
                        description: |-
                          Human-readable message with details about the last transition.
                        maxLength: 32768
                        type: string
                      observedGeneration:
                        description: |-
                          `.metadata.generation` the condition was based upon.
                          For instance, if `.metadata.generation` is currently `12` but `.status.conditions[x].observedGeneration` is `9`, the condition is considered outdated.
                        format: int64
                        minimum: 0
                        type: integer
                      reason:
                        description: |-
                          Brief reason for the condition's last transition.
                        maxLength: 1024
                        minLength: 1
                        pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                        type: string
                      status:
                        description: |-
                          Condition status. Possible values: `True`, `False`, or `Unknown`.
                        enum:
                          - "True"
                          - "False"
                          - Unknown
                        type: string
                      type:
                        description: Condition type.
                        maxLength: 316
                        pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                        type: string
                    required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                    type: object
                  type: array
                observedGeneration:
                  description: Resource generation last processed by the controller.
                  format: int64
                  type: integer
                phase:
                  description: |-
                    Current phase of the resource:

                    * `Pending`: The operation is queued for execution.
                    * `InProgress`: The operation is in progress.
                    * `Completed`: The operation has been completed successfully.
                    * `Failed`: The operation failed. For details, refer to the `conditions` field and events.
                    * `Terminating`: The operation is being deleted.
                  enum:
                    - Pending
                    - InProgress
                    - Completed
                    - Failed
                    - Terminating
                  type: string
              required:
                - phase
              type: object
          required:
            - spec
          type: object
      served: true
      storage: true
      subresources:
        status: {}
