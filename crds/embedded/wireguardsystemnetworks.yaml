---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  labels:
    heritage: deckhouse
    module: virtualization
  name: wireguardsystemnetworks.usbgateway.virtualization.deckhouse.io
spec:
  group: usbgateway.virtualization.deckhouse.io
  names:
    kind: WireguardSystemNetwork
    listKind: WireguardSystemNetworkList
    plural: wireguardsystemnetworks
    shortNames:
    - wsn
    singular: wireguardsystemnetwork
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              cidr:
                format: cidr
                type: string
              interfaceName:
                type: string
              listenPort:
                type: integer
              persistentKeepalive:
                type: integer
            required:
            - cidr
            - interfaceName
            - listenPort
            type: object
          status:
            properties:
              allocatedIPs:
                items:
                  properties:
                    ip:
                      type: string
                    node:
                      type: string
                  required:
                  - ip
                  - node
                  type: object
                type: array
              nodeSettings:
                items:
                  properties:
                    endpoint:
                      type: string
                    node:
                      type: string
                    privateKey:
                      type: string
                    publicKey:
                      type: string
                  required:
                  - endpoint
                  - node
                  - privateKey
                  - publicKey
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
