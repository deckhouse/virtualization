---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  labels:
    heritage: deckhouse
    module: virtualization
  name: virtualdataexports.virtualization.deckhouse.io
spec:
  group: virtualization.deckhouse.io
  names:
    categories:
      - virtualization
      - all
    kind: VirtualDataExport
    listKind: VirtualDataExportList
    plural: virtualdataexports
    shortNames:
      - vde
      - vdes
    singular: virtualdataexport
  scope: Namespaced
  versions:
    - additionalPrinterColumns:
        - description: VirtualDataExport completion status.
          jsonPath: .status.conditions[?(@.type=='Completed')].reason
          name: Completed
          type: string
        - jsonPath: .metadata.creationTimestamp
          name: Age
          type: date
      name: v1alpha2
      schema:
        openAPIV3Schema:
          description:
            The VirtualDataExport describes the configuration and status
            of a virtual data export (VDExport).
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              properties:
                idleTimeout:
                  default: 30m
                  description: |-
                    IdleTimeout defines the maximum duration to wait for a user to initiate the download.
                    The export is considered **Expired** if:
                      1. The user never initiated the download, **AND**
                      2. The specified (or default) `.spec.idleTimeout` period has elapsed.
                    Default: "30m" (30 minutes).
                    Must be a valid duration string (e.g., "1h", "30m", "60s").
                  format: duration
                  type: string
                targetRef:
                  description: |-
                    TargetRef defines the destination where data will be exported.
                    Contains reference information about the export target.
                  properties:
                    kind:
                      description:
                        VirtualDataExportTargetKind defines the supported
                        types of export targets.
                      enum:
                        - VirtualDisk
                        - VirtualDiskSnapshot
                        - VirtualImage
                        - ClusterVirtualImage
                      type: string
                    name:
                      description: |-
                        Name identifies the specific instance of the target resource.
                        This should match the metadata.name of the referenced resource.
                      type: string
                  required:
                    - kind
                    - name
                  type: object
              required:
                - targetRef
              type: object
            status:
              description:
                VirtualDataExportStatus defines the observed state of a VirtualDataExport
                resource.
              properties:
                conditions:
                  description:
                    The latest available observations of an object's current
                    state.
                  items:
                    description:
                      Condition contains details for one aspect of the current
                      state of this API Resource.
                    properties:
                      lastTransitionTime:
                        description: |-
                          lastTransitionTime is the last time the condition transitioned from one status to another.
                          This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                        format: date-time
                        type: string
                      message:
                        description: |-
                          message is a human readable message indicating details about the transition.
                          This may be an empty string.
                        maxLength: 32768
                        type: string
                      observedGeneration:
                        description: |-
                          observedGeneration represents the .metadata.generation that the condition was set based upon.
                          For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                          with respect to the current state of the instance.
                        format: int64
                        minimum: 0
                        type: integer
                      reason:
                        description: |-
                          reason contains a programmatic identifier indicating the reason for the condition's last transition.
                          Producers of specific condition types may define expected values and meanings for this field,
                          and whether the values are considered a guaranteed API.
                          The value should be a CamelCase string.
                          This field may not be empty.
                        maxLength: 1024
                        minLength: 1
                        pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                        type: string
                      status:
                        description: status of the condition, one of True, False, Unknown.
                        enum:
                          - "True"
                          - "False"
                          - Unknown
                        type: string
                      type:
                        description: type of condition in CamelCase or in foo.example.com/CamelCase.
                        maxLength: 316
                        pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                        type: string
                    required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                    type: object
                  type: array
                observedGeneration:
                  description: Resource generation last processed by the controller.
                  format: int64
                  type: integer
                url:
                  description: |-
                    The URL represents the download endpoint where the exported data can be accessed.
                    This field is populated when the export is ready for download.
                    The URL is typically valid until the export expires or is deleted.
                  type: string
              type: object
          required:
            - spec
          type: object
      served: true
      storage: true
      subresources:
        status: {}
