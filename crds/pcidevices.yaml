---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  labels:
    heritage: deckhouse
    module: virtualization
  name: pcidevices.virtualization.deckhouse.io
spec:
  group: virtualization.deckhouse.io
  names:
    categories:
      - virtualization
    kind: PCIDevice
    listKind: PCIDeviceList
    plural: pcidevices
    shortNames:
      - pci
    singular: pcidevice
  scope: Namespaced
  versions:
    - additionalPrinterColumns:
        - jsonPath: .status.nodeName
          name: Node
          type: string
        - jsonPath: .status.attributes.pciAddress
          name: Address
          type: string
        - jsonPath: .status.attributes.vendorID
          name: VendorID
          priority: 1
          type: string
        - jsonPath: .status.attributes.deviceID
          name: DeviceID
          priority: 1
          type: string
        - jsonPath: .status.attributes.class
          name: Class
          priority: 1
          type: string
        - jsonPath: .status.conditions[?(@.type=="Attached")].status
          name: Attached
          type: string
        - jsonPath: .metadata.creationTimestamp
          name: Age
          type: date
      name: v1alpha2
      schema:
        openAPIV3Schema:
          description:
            Represents a PCI device available for attachment to virtual machines
            in a given namespace.
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            status:
              description: Observed state of `PCIDevice`.
              properties:
                attributes:
                  description: All device attributes obtained through DRA for the device.
                  properties:
                    class:
                      description: PCI class code in hexadecimal format.
                      type: string
                    deviceID:
                      description: PCI device ID in hexadecimal format.
                      type: string
                    driver:
                      description: Kernel driver bound to the device on the host.
                      type: string
                    iommuGroup:
                      description:
                        IOMMU group number the device belongs to. The whole
                        group is passed to the virtual machine.
                      format: int64
                      type: integer
                    name:
                      description: Device name.
                      type: string
                    nodeName:
                      description: Node name where the device is located.
                      type: string
                    numaNode:
                      description:
                        NUMA node of the device; -1 when the platform reports
                        none.
                      format: int64
                      type: integer
                    pciAddress:
                      description:
                        PCI address of the device in extended BDF notation,
                        for example 0000:3b:00.0.
                      type: string
                    vendorID:
                      description: PCI vendor ID in hexadecimal format.
                      type: string
                  type: object
                conditions:
                  description:
                    Latest available observations of an object's current
                    state.
                  items:
                    description:
                      Condition contains details for one aspect of the current
                      state of this API Resource.
                    properties:
                      lastTransitionTime:
                        description: |-
                          lastTransitionTime is the last time the condition transitioned from one status to another.
                          This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                        format: date-time
                        type: string
                      message:
                        description: |-
                          message is a human readable message indicating details about the transition.
                          This may be an empty string.
                        maxLength: 32768
                        type: string
                      observedGeneration:
                        description: |-
                          observedGeneration represents the .metadata.generation that the condition was set based upon.
                          For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                          with respect to the current state of the instance.
                        format: int64
                        minimum: 0
                        type: integer
                      reason:
                        description: |-
                          reason contains a programmatic identifier indicating the reason for the condition's last transition.
                          Producers of specific condition types may define expected values and meanings for this field,
                          and whether the values are considered a guaranteed API.
                          The value should be a CamelCase string.
                          This field may not be empty.
                        maxLength: 1024
                        minLength: 1
                        pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                        type: string
                      status:
                        description: status of the condition, one of True, False, Unknown.
                        enum:
                          - "True"
                          - "False"
                          - Unknown
                        type: string
                      type:
                        description: type of condition in CamelCase or in foo.example.com/CamelCase.
                        maxLength: 316
                        pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                        type: string
                    required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                    type: object
                  type: array
                nodeName:
                  description: Name of the node where the PCI device is located.
                  type: string
                observedGeneration:
                  description: Resource generation last processed by the controller.
                  format: int64
                  type: integer
              type: object
          type: object
          x-doc-examples:
            - apiVersion: virtualization.deckhouse.io/v1alpha2
              kind: PCIDevice
              metadata:
                name: example-pci
      served: true
      storage: true
      subresources:
        status: {}
