---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  labels:
    heritage: deckhouse
    module: virtualization
  name: virtualmachineblockdeviceattachments.virtualization.deckhouse.io
spec:
  group: virtualization.deckhouse.io
  names:
    categories:
      - all
      - virtualization
    kind: VirtualMachineBlockDeviceAttachment
    listKind: VirtualMachineBlockDeviceAttachmentList
    plural: virtualmachineblockdeviceattachments
    shortNames:
      - vmbda
      - vmbdas
    singular: virtualmachineblockdeviceattachment
  scope: Namespaced
  versions:
    - additionalPrinterColumns:
        - description: VirtualMachineBlockDeviceAttachment phase.
          jsonPath: .status.phase
          name: PHASE
          type: string
        - description: Name of the virtual machine the disk is attached to.
          jsonPath: .status.virtualMachineName
          name: VIRTUAL MACHINE NAME
          type: string
        - description: Time of resource creation.
          jsonPath: .metadata.creationTimestamp
          name: AGE
          type: date
      name: v1alpha2
      schema:
        openAPIV3Schema:
          description:
            Provides a hot plug for attaching a disk to a virtual machine (VM).
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                Must be written in CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              properties:
                blockDeviceRef:
                  description:
                    Block device that will be connected to the VM as a hot-plug disk.
                  properties:
                    kind:
                      description: |-
                        Block device type. Available types:
                        * `VirtualDisk`: Use VirtualDisk as a disk. This type is always mounted in RW mode.
                      enum:
                        - VirtualDisk
                      type: string
                    name:
                      description: Block device name.
                      type: string
                  type: object
                virtualMachineName:
                  description:
                    Virtual machine name the disk is attached to.
                  type: string
              required:
                - blockDeviceRef
                - virtualMachineName
              type: object
            status:
              properties:
                conditions:
                  description: Last confirmed status of the resource.
                  items:
                    description:
                      Details of a single aspect of the current status for this API resource.
                    properties:
                      lastTransitionTime:
                        description: |-
                          Last time the condition transitioned from one status to another.
                        format: date-time
                        type: string
                      message:
                        description: |-
                          Human-readable message with details about the last transition.
                        maxLength: 32768
                        type: string
                      observedGeneration:
                        description: |-
                          `.metadata.generation` the condition was based upon.
                          For instance, if `.metadata.generation` is currently `12` but `.status.conditions[x].observedGeneration` is `9`, the condition is considered outdated.
                        format: int64
                        minimum: 0
                        type: integer
                      reason:
                        description: |-
                          Brief reason for the condition's last transition.
                        maxLength: 1024
                        minLength: 1
                        pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                        type: string
                      status:
                        description: |-
                          Condition status. Possible values: `True`, `False`, or `Unknown`.
                        enum:
                          - "True"
                          - "False"
                          - Unknown
                        type: string
                      type:
                        description: Condition type.
                        maxLength: 316
                        pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                        type: string
                    required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                    type: object
                  type: array
                observedGeneration:
                  description: Resource generation last processed by the controller.
                  format: int64
                  type: integer
                phase:
                  description: |-
                    Resource phase:

                    * `Pending`: The resource has been created and is on a waiting queue.
                    * `InProgress`: The disk is being attached to the VM.
                    * `Attached`: The disk has been attached to the VM.
                    * `Failed`: There was an error when attaching the disk.
                    * `Terminating`: The resource is being deleted.
                  enum:
                    - Pending
                    - InProgress
                    - Attached
                    - Failed
                    - Terminating
                  type: string
                virtualMachineName:
                  description:
                    Virtual machine name the disk is attached to.
                  type: string
              type: object
          required:
            - spec
          type: object
      served: true
      storage: true
      subresources:
        status: {}
